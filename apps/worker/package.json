{
  "name": "@chorus/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/worker.ts",
    "build": "tsc",
    "start": "node dist/worker.js",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@chorus/shared-types": "*",
    "@chorus/tree-sitter-utils": "*",
    "@chorus/graph-utils": "*",
    "bullmq": "^5.0.0",
    "ioredis": "^5.3.0",
    "pino": "^8.18.0"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
