'use client';
import React from 'react';
export const BranchHeatmap: React.FC<{ repoId: string }> = ({ repoId }) => (
  <div className="absolute inset-0 pointer-events-none">
    {/* Branch activity heatmap overlay */}
  </div>
);
